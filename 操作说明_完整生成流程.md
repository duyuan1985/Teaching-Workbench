# 教学档案工作台 — 完整操作说明

## 概述

从教学安排表到最终生成三份文档（课程标准、授课计划、教学设计），共分 **7 个步骤**。
步骤1-3为数据准备，步骤4-5为AI内容生成，步骤6为文档生成，步骤7为批量精修。

---

## 前置条件

1. **Python 环境**：已安装 python-docx、openpyxl 等依赖
2. **原始资料**：放在 `原始资料/` 目录下
   - 教学安排表（`原始资料/教学安排表/*.xlsx`）
   - 学期进程表（`原始资料/学期进程表/*.xlsx`）
   - 学校校历（`原始资料/学校校历/*.xls`）
   - 教材资源（`原始资料/教材/<课程名>/`）
   - 人才培养方案（`原始资料/人才培养方案/`）
3. **模板文件**：已通过 `template_analyzer.py` 解析入库
4. **AI 配置**（步骤5必需）：
   - 环境变量 `ZHIPU_API_KEY` 已设置
   - 数据库 `enhanced_generation = 1`

---

## 步骤总览

| 步骤 | 脚本 | 说明 | 耗时 |
|------|------|------|------|
| 1 | `bootstrap_import.py` | 导入教学安排表，创建开课记录 | 1-2分钟 |
| 2 | `course_standard_model.py` | 建立课程蓝本（curriculum_units） | 2-3分钟/课程 |
| 3 | `task_builder.py` | 创建教学任务（tasks） | 几秒/课程 |
| 4 | `semantic_model.py` | 构建语义模型 | 几秒/课程 |
| 5 | `content_author.py` | AI生成内容（调用智谱GLM） | 5-6分钟/课程 |
| 6 | `generate.py` | 生成文档（.docx） | 1-2分钟/课程 |
| 7 | `_batch_polish.py` | 批量精修格式（行高/空段落/行距/字体） | 2-3分钟 |

---

## 详细操作

### 步骤1：导入教学安排表

```bash
python bootstrap_import.py
```

**做什么：**
- 扫描 `原始资料/教学安排表/*.xlsx`
- 按（学期 × 课程 × 专业）创建 `offerings` 记录
- 导入排课记录（`sessions` 表）
- 导入学期进程表（上课日期）
- 导入校历（无进程表时先按校历周次生成上课日期，再处理停课/调课；缺少的课程学时自动安排到第18周并直接确认）
- 索引教材资源（PPT/大纲/源码）
- 登记人才培养方案
- 自动匹配模板文件

**输出：** JSON 格式的创建/跳过/导入/失败摘要

**界面操作：**
- 课程详情页的“重建排课”与本脚本共用 `import_schedule_bundle()`，执行顺序和结果一致。
- 已登记教学安排表、学期进程表、学校校历后，点击“重建排课”即可重新计算日期并应用校历事件。
- 没有学期进程表但校历包含周次日期时，系统会直接用校历生成每次课的实际日期。
- 校历处理后如班级已确认学时不足课程总学时，差额按周学时拆分为第18周补课并直接确认。

> 当前整合状态：排课脚本与界面“重建排课”已统一；普通课程整本生成时，界面与命令行均调用 `generate.py` 及 `gen_standard.py`、`gen_plan.py`、`gen_design.py`。通用精修已改为仅处理本次生成文件并自动执行。指定周次教学设计和实训资料继续使用各自专用生成分支。
**注意事项：**
- 重复运行会跳过已存在的课程（加 `force=True` 可强制重新导入）
- 导入后需要检查 sessions 的 status 字段，手动确认"待确认"的记录

---

### 步骤2：建立课程蓝本（curriculum_units）

```bash
python -c "
import course_standard_model
import store

# 查看需要处理的课程
offerings = store.rows('SELECT id, course_name, term FROM offerings WHERE offering_kind != \"实训课程\" ORDER BY id')
for o in offerings:
    units = store.rows('SELECT COUNT(*) as cnt FROM curriculum_units WHERE offering_id=?', [o['id']])
    print(f'ID={o[\"id\"]} {o[\"course_name\"]} ({o[\"term\"]}): {units[0][\"cnt\"]}个units')
"
```

**做什么：**
- 从教材PPT大纲提取章节结构
- 创建 `curriculum_units` 记录（项目标题、学时、技能点）
- 对应教材的章节划分

**手动确认：**
```bash
# 把所有units状态改为"已确认"
python -c "
import store
with store.connect() as db:
    db.execute(\"UPDATE curriculum_units SET approval_status='已确认' WHERE approval_status='待确认'\")
    db.commit()
print('全部已确认')
"
```

---

### 步骤3：创建教学任务（tasks）

```bash
# 单个课程
python -c "
import store, task_builder
o = store.rows('SELECT * FROM offerings WHERE id=?', [18])[0]
count = task_builder.build_tasks(o)
print(f'创建了{count}个任务')
"

# 批量创建所有课程
python -c "
import store, task_builder
offerings = store.rows('SELECT * FROM offerings WHERE offering_kind != \"实训课程\" ORDER BY id')
for o in offerings:
    existing = store.rows('SELECT COUNT(*) as cnt FROM tasks WHERE offering_id=?', [o['id']])
    if existing[0]['cnt'] > 0:
        print(f'  ID={o[\"id\"]} {o[\"course_name\"]}: 已有{existing[0][\"cnt\"]}个任务，跳过')
        continue
    try:
        count = task_builder.build_tasks(o)
        print(f'  ID={o[\"id\"]} {o[\"course_name\"]}: 创建了{count}个任务')
    except Exception as e:
        print(f'  ID={o[\"id\"]} {o[\"course_name\"]}: 失败 - {e}')
"
```

**做什么：**
- 从 `curriculum_units` 拆分教学任务
- 按 `sessions` 排课日期分配任务到每周
- 生成每个任务的知识目标、能力目标、思政目标、素质目标

**前置条件：**
- `curriculum_units.approval_status = '已确认'`
- `sessions.status = '已确认'` 且学时总数 = `offerings.total_hours`

---

### 步骤4：构建语义模型

```bash
# 单个课程
python -c "
import store, semantic_model
model = semantic_model.build_semantic_model(18)
print(f'项目数: {len(model.get(\"projects\", []))}')
print(f'知识点: {len(model.get(\"knowledge_system\", []))}')
"

# 批量构建
python -c "
import store, semantic_model
offerings = store.rows('SELECT id, course_name FROM offerings WHERE offering_kind != \"实训课程\" ORDER BY id')
for o in offerings:
    existing = store.rows('SELECT COUNT(*) as cnt FROM course_content_models WHERE offering_id=?', [o['id']])
    if existing[0]['cnt'] > 0:
        print(f'  ID={o[\"id\"]} {o[\"course_name\"]}: 已有模型，跳过')
        continue
    try:
        model = semantic_model.build_semantic_model(o['id'])
        print(f'  ID={o[\"id\"]} {o[\"course_name\"]}: {len(model.get(\"projects\", []))}项目, {len(model.get(\"knowledge_system\", []))}知识点')
    except Exception as e:
        print(f'  ID={o[\"id\"]} {o[\"course_name\"]}: 失败 - {e}')
"
```

**做什么：**
- 从 `curriculum_units`、`resource_facts`、`talent_plan` 汇总课程语义信息
- 生成 `course_content_models.model_json`
- 包含：课程身份、项目列表、知识体系、工具技术、标准、工作过程、教学方法、考核方案

**前置条件：**
- `curriculum_units` 已确认
- 教材资源已索引（`resource_items` 有数据）

---

### 步骤5：AI生成内容（智谱GLM）

```bash
# 开启AI增强
python -c "
import store
with store.connect() as db:
    db.execute(\"UPDATE settings SET setting_value='1' WHERE setting_key='enhanced_generation'\")
    db.commit()
print('AI增强已开启')
"

# 单个课程
python -c "
import content_author
count = content_author.author_course_content(18)
print(f'生成了{count}个section')
"

# 批量生成（跳过已有AI内容的课程）
python -c "
import store, content_author, time
offerings = store.rows('SELECT id, course_name, term FROM offerings WHERE offering_kind != \"实训课程\" ORDER BY id')
for o in offerings:
    existing = store.rows('SELECT COUNT(*) as cnt FROM authored_sections WHERE offering_id=?', [o['id']])
    if existing[0]['cnt'] > 0:
        print(f'  ID={o[\"id\"]} {o[\"course_name\"]}: 已有{existing[0][\"cnt\"]}个section，跳过')
        continue
    print(f'\n=== {o[\"course_name\"]} ({o[\"term\"]}) ===')
    t0 = time.time()
    try:
        count = content_author.author_course_content(o['id'])
        elapsed = time.time() - t0
        print(f'  完成: {count}个section, 耗时{elapsed:.0f}秒')
    except Exception as e:
        print(f'  失败: {e}')
"
```

**做什么：**
- 读取 `course_content_models` 语义模型
- 调用智谱GLM（在线）或Ollama（本地）生成：
  - 课程概述（课程性质、课程目标、课程设计思路）
  - 每个任务的教学活动（教学导入、任务1/2/3、课堂小结、课后作业、教学反思）
- AI失败时自动回退到规则生成（`authoring_status = '结构化生成'`）

**AI连接方式：**
- 在线：智谱GLM-4-Flash，需要环境变量 `ZHIPU_API_KEY`
- 本地：Ollama qwen3:8b，需要 `http://127.0.0.1:11434`
- 优先级由 `ai_model_preference` 控制（`online`/`local`/`auto`）

**输出：** 写入 `authored_sections` 表

---

### 步骤6：生成文档

```bash
# 单个课程，生成全部三份文档
python generate.py 18 --doc all

# 单个课程，只生成课程标准
python generate.py 18 --doc standard

# 单个课程，指定输出目录
python generate.py 18 --doc all --output "生成结果/测试"

# 批量生成所有课程
python -c "
import store, generate
offerings = store.rows('SELECT id, course_name, term FROM offerings WHERE offering_kind != \"实训课程\" ORDER BY id')
for o in offerings:
    print(f'\n=== ID={o[\"id\"]} {o[\"course_name\"]} ({o[\"term\"]}) ===')
    try:
        results = generate.generate_all(o['id'])
        for dt, r in results.items():
            if 'path' in r:
                print(f'  {dt}: {r[\"path\"]}')
            elif 'error' in r:
                print(f'  {dt}: 错误 - {r[\"error\"]}')
    except Exception as e:
        print(f'  失败: {e}')
"
```

**做什么：**
- 读取模板 → 填充 `authored_sections` 和 `tasks` 内容
- 生成课程标准（`gen_standard.py`）
- 生成授课计划（`gen_plan.py`）
- 生成教学设计（`gen_design.py` 增强版）
- 自动验证：空格率、模板残留

**输出路径：** `生成结果/精修版/{学期}《{课程名}》{文档类型} {教师}.docx`

**同名课程防覆盖：** 当同一学期同一课程有多个专业/班级时，文件名自动添加专业后缀：
- `2024-2025-1《图形图像设计》课程标准 杜媛（全媒体）.docx`
- `2024-2025-1《图形图像设计》课程标准 杜媛（农商）.docx`

**模板版本兼容（gen_standard.py）：**
- 旧模板（2023-2024、2024-2025-1）：4个表格，表1为内容表（6列）
- 新模板（2024-2025-2、2025-2026）：6个表格，表3为内容表（4列），表5为考核表（4列）
- 代码自动检测 `len(doc.tables) >= 6` 切换处理逻辑

**gen_design.py 增强版说明：**

增强版 gen_design.py 已将原来 _fix_*.py 系列脚本的核心逻辑集成到生成流程中，一次性输出完整的教学设计文档：

| 增强功能 | 对应的旧脚本 | 说明 |
|---------|-------------|------|
| 封面填充（段落+文本框） | _fix_textbox.py | 填充校名/课程/教师/班级/学期到封面段落和 w:txbxContent 文本框 |
| 表0课程信息表 | _fix_design_r4.py | 填充课程名/代码/系部/教师/学期/先修后续课 |
| 表1课程内容设计 | _fix_design_r4.py | 填充第一章~第九章+学时，按章节分组 |
| 表2职业能力训练 | _fix_design_r4.py | 每个教学任务一行（30行），含能力目标/知识点/训练方式 |
| 表3教学进度表 | _fix_design_r4.py | 每个教学任务一行（30行），含周次/日期/课题/目标 |
| 表4考核方案 | _fix_t4_assessment.py | 过程性40%（签到10%+课堂表现10%+作业20%）+ 终结性60%（综合作品），分行显示并合并单元格 |
| 表5+6克隆30对 | _fix_design_r2.py + r3.py | 深拷贝模板表5/表6的XML元素，为每个教学任务创建一对表格，含"教学设计·基本信息"和"教学设计·教学组织"标题段落 |
| AI内容接入 | _fix_activity_flow.py + _fix_all_goals.py | 从 authored_sections 加载 unit_design 内容（教学目标/教学导入/任务1-3/课堂小结/课后作业/教学反思），填入对应的表5和表6 |

**gen_design.py 新增函数：**
| 函数 | 功能 |
|------|------|
| `_fill_cover(doc, offering)` | 填充封面段落和文本框 |
| `_fill_table0(t, offering, units)` | 填充课程信息表 |
| `_fill_table1(t, offering, tasks, units)` | 填充课程内容设计表（按章节分组） |
| `_fill_table2(t, offering, tasks, units)` | 填充职业能力训练表（每任务一行） |
| `_fill_table3(t, offering, tasks, sessions)` | 填充教学进度表（每任务一行） |
| `_fill_table4(t, offering)` | 填充考核方案表（过程性+终结性，分行显示） |
| `_clone_task_tables(doc, n_tasks)` | 克隆表5+6对（深拷贝XML，插入标题段落） |
| `_load_ai_content(offering_id)` | 从 authored_sections 加载 unit_design 内容 |
| `_fill_task_basic_info(t, ...)` | 填充单个任务的表5（使用AI内容） |
| `_fill_task_teaching_org(t, ...)` | 填充单个任务的表6（使用AI内容） |

**已知限制：** 模板表5（16行×20列）和表6（11行×6列）有大量合并单元格，当前 cell(row,col) 映射与备份文档存在位置偏移，后续需要分析模板合并布局并修正映射。

---

### 步骤7：批量精修

```bash
# 批量精修所有文档（行高/空段落/行距/字体/模板残留）
python _batch_polish.py
```

**做什么：**

| 修复项 | 说明 |
|--------|------|
| 行高自适应 | 所有表格行高从固定值改为 `auto`，防止内容被截断 |
| 空段落清除 | 删除表格单元格中多余的空段落（保留至少1个） |
| 行距修复 | 固定行距（`exact`）改为 `auto`，防止大字号文字被切 |
| 字号统一 | `sz` 和 `szCs` 值同步，防止显示不一致 |
| 空段落缩进清除 | 空段落的大缩进（`firstLineChars > 10`）清除 |
| 模板残留清除 | 清除"××××课程是××××专业"等模板说明文字 |
| 同名课程重新生成 | 为被覆盖的课程重新生成带专业后缀的文件 |

**精修脚本结构（`_batch_polish.py`）：**
1. `regen_overwritten_courses()` — 重新生成被覆盖的同名课程（带专业后缀）
2. `clean_template_residuals()` — 清除课标模板残留说明文字
3. `apply_all_format_fixes()` — 对所有文档统一应用格式修复

**单文档精修（可选）：**

`_fix_*.py` 系列脚本中，内容补充类的逻辑已集成到增强版 gen_design.py 中（见步骤6说明）。剩余格式修复类的逻辑已集成到 `_batch_polish.py` 中。以下脚本仅作为历史参考，不再需要单独运行：

**已集成到 gen_design.py（内容补充类）：**
- `_fix_textbox.py` → 封面文本框填充（→ `_fill_cover()`）
- `_fix_design_r2.py` → 表5+6克隆30对（→ `_clone_task_tables()`）
- `_fix_design_r3.py` → 表5/6 XML结构重写（→ `_clone_task_tables()`）
- `_fix_design_r4.py` → 封面/表0/表1/表5教学要求（→ `_fill_cover()` + `_fill_table0/1()` + `_fill_task_basic_info()`）
- `_fix_activity_flow.py` → 教学组织表R5/R6/R7扩展（→ `_fill_task_teaching_org()`）
- `_fix_all_goals.py` → 30个任务教学目标（→ `_fill_task_basic_info()` + AI内容）
- `_fix_t5_headers.py` → 表5标题行修复（→ `_fill_task_basic_info()`）
- `_fix_t4_assessment.py` → 考核方案表修复（→ `_fill_table4()`）

**已集成到 _batch_polish.py（格式修复类）：**
- `_fix_all_heights.py` — 行高改为auto
- `_fix_empty_paras.py` — 清除表格空段落
- `_fix_row_height.py` — 教学组织表行高
- `_fix_design_font.py` — 表5/表6字体统一仿宋五号

---

## 快速参考：一键批量执行

### 方式一：分步批量执行（推荐）

```bash
# 步骤4+5+6 批量执行（跳过已有模型/AI内容的课程，约90分钟）
python _batch_full_pipeline.py
```

**脚本说明（`_batch_full_pipeline.py`）：**
- 自动检测已有语义模型和AI内容，跳过已完成的课程
- 每门课程依次执行：语义模型 → AI内容生成 → 文档生成
- 失败时自动继续下一门课程
- 输出详细日志和汇总报告

### 方式二：完整流程一键执行

```bash
python -c "
import store, semantic_model, content_author, generate, task_builder, time

# 获取所有需要处理的课程
offerings = store.rows('SELECT * FROM offerings WHERE offering_kind != \"实训课程\" ORDER BY id')
total = len(offerings)
print(f'共{total}个课程需要处理')

for i, o in enumerate(offerings, 1):
    oid = o['id']
    print(f'\n[{i}/{total}] ID={oid} {o[\"course_name\"]} ({o[\"term\"]})')

    # 步骤2: 确认curriculum_units
    with store.connect() as db:
        db.execute('UPDATE curriculum_units SET approval_status=? WHERE offering_id=? AND approval_status=?',
                   ('已确认', oid, '待确认'))
        db.commit()

    # 步骤3: 创建任务（跳过已有的）
    existing = store.rows('SELECT COUNT(*) as cnt FROM tasks WHERE offering_id=?', [oid])
    if existing[0]['cnt'] == 0:
        try:
            count = task_builder.build_tasks(o)
            print(f'  任务: 创建了{count}个')
        except Exception as e:
            print(f'  任务: 失败 - {e}')
            continue
    else:
        print(f'  任务: 已有{existing[0][\"cnt\"]}个，跳过')

    # 步骤4: 语义模型
    try:
        model = semantic_model.build_semantic_model(oid)
        print(f'  模型: {len(model.get(\"projects\", []))}项目')
    except Exception as e:
        print(f'  模型: 失败 - {e}')
        continue

    # 步骤5: AI内容
    t0 = time.time()
    try:
        count = content_author.author_course_content(oid)
        print(f'  AI内容: {count}个section ({time.time()-t0:.0f}秒)')
    except Exception as e:
        print(f'  AI内容: 失败 - {e}')

    # 步骤6: 生成文档
    try:
        results = generate.generate_all(oid)
        for dt, r in results.items():
            if 'path' in r:
                print(f'  {dt}: OK')
            elif 'error' in r:
                print(f'  {dt}: 错误 - {r[\"error\"]}')
    except Exception as e:
        print(f'  生成: 失败 - {e}')

# 步骤7: 批量精修
print('\n=== 步骤7: 批量精修 ===')
exec(open('_batch_polish.py').read())

print('\n全部完成!')
"
```

---

## 脚本清单

### 核心脚本

| 脚本 | 用途 | 调用方式 |
|------|------|----------|
| `bootstrap_import.py` | 步骤1：导入教学安排表 | `python bootstrap_import.py` |
| `course_standard_model.py` | 步骤2：建立课程蓝本 | `python -c "import course_standard_model"` |
| `task_builder.py` | 步骤3：创建教学任务 | `python -c "import task_builder"` |
| `semantic_model.py` | 步骤4：构建语义模型 | `python -c "import semantic_model"` |
| `content_author.py` | 步骤5：AI生成内容 | `python -c "import content_author"` |
| `generate.py` | 步骤6：生成文档 | `python generate.py <id> --doc all` |
| `gen_standard.py` | 步骤6子模块：课程标准 | 由 generate.py 调用 |
| `gen_plan.py` | 步骤6子模块：授课计划 | 由 generate.py 调用 |
| `gen_design.py` | 步骤6子模块：教学设计 | 由 generate.py 调用 |
| `_batch_polish.py` | 步骤7：批量精修格式 | `python _batch_polish.py` |

### 批量执行脚本

| 脚本 | 用途 | 调用方式 |
|------|------|----------|
| `_batch_full_pipeline.py` | 批量执行步骤4+5+6 | `python _batch_full_pipeline.py` |
| `_regen_standards.py` | 重新生成新模板课程标准 | `python _regen_standards.py` |

### 单项精修脚本（_fix_*.py，历史参考）

以下脚本的功能已集成到 gen_design.py（内容补充类）或 `_batch_polish.py`（格式修复类）中，不再需要单独运行。保留作为历史参考：

**已集成到 gen_design.py（内容补充类）：**
| 脚本 | 集成到 | 功能 |
|------|--------|------|
| `_fix_textbox.py` | `_fill_cover()` | 封面文本框填充 |
| `_fix_design_r2.py` | `_clone_task_tables()` | 表5+6克隆30对 |
| `_fix_design_r3.py` | `_clone_task_tables()` | 表5/6 XML结构重写 |
| `_fix_design_r4.py` | `_fill_cover()` + `_fill_table0/1()` | 封面/表0/表1/表5教学要求 |
| `_fix_activity_flow.py` | `_fill_task_teaching_org()` | 教学组织表内容扩展 |
| `_fix_all_goals.py` | `_fill_task_basic_info()` | 30个任务教学目标 |
| `_fix_t5_headers.py` | `_fill_task_basic_info()` | 表5标题行修复 |
| `_fix_t4_assessment.py` | `_fill_table4()` | 考核方案表修复 |

**已集成到 _batch_polish.py（格式修复类）：**
| 脚本 | 功能 |
|------|------|
| `_fix_all_heights.py` | 所有表格行高改为auto |
| `_fix_empty_paras.py` | 清除表格单元格空段落 |
| `_fix_row_height.py` | 教学组织表行高 |
| `_fix_design_font.py` | 表5/表6字体统一仿宋五号 |

**未集成（按需手动使用）：**
| 脚本 | 功能 | 说明 |
|------|------|------|
| `_fix_goals.py` | 课程目标与课标一致 | 需手动运行 |
| `_fix_methods.py` | 教学方法与课标一致 | 需手动运行 |
| `_fix_textbook.py` | 统一教材描述 | 需手动运行 |
| `_fix_t3_truncated.py` | 教学进度表截断内容修复 | 需手动运行 |
| `_fix_weights.py` | 考核权重修改 | 需手动运行 |

**数据修复类（按需使用）：**
| 脚本 | 功能 |
|------|------|
| `_fix_28.py` | ID=28 第17周多出课时取消 |
| `_fix_33_34.py` | ID=33 第18周补课记录 |
| `_fix_multiclass_makeup.py` | 多班课程补课学时补齐 |

---

## 数据库核心表关系

```
offerings（开课记录）
  ├── sessions（排课记录）
  ├── curriculum_units（课程蓝本/章节）
  ├── tasks（教学任务）
  ├── course_content_models（语义模型JSON）
  ├── authored_sections（AI生成的内容）
  ├── template_files（模板文件）
  ├── resource_items（教材资源索引）
  │     └── resource_facts（资源事实提取）
  ├── source_files（源文件登记）
  └── quality_issues（质量检查问题）
```

---

## 常见问题

### Q: sessions学时不等于total_hours怎么办？
```bash
# 检查学时差异
python -c "
import store
offerings = store.rows('SELECT * FROM offerings WHERE offering_kind != \"实训课程\" ORDER BY id')
for o in offerings:
    sessions = store.rows('SELECT * FROM sessions WHERE offering_id=? AND status=\"已确认\" AND session_type<>\"停课\"', [o['id']])
    total = sum(s['hours'] for s in sessions)
    if total != o['total_hours']:
        print(f'ID={o[\"id\"]} {o[\"course_name\"]}: {total}/{o[\"total_hours\"]} (差{o[\"total_hours\"]-total})')
"
```

### Q: AI生成失败怎么办？
- AI失败会自动回退到规则生成（`authoring_status = '结构化生成'`）
- 检查 `ZHIPU_API_KEY` 是否有效
- 检查网络连接
- 可以单独重试某个课程

### Q: 如何重新生成某个课程的AI内容？
```bash
python -c "
import store, content_author
oid = 18  # 课程ID
with store.connect() as db:
    db.execute('DELETE FROM authored_sections WHERE offering_id=?', [oid])
    db.commit()
count = content_author.author_course_content(oid)
print(f'重新生成了{count}个section')
"
```

### Q: 如何只重新生成文档（不重新生成AI内容）？
```bash
python generate.py 18 --doc all
```

### Q: 课程标准生成报错"tuple index out of range"？
这是模板版本不匹配导致的。`gen_standard.py` 已支持新旧两种模板：
- 旧模板（4个表格）：2023-2024、2024-2025-1
- 新模板（6个表格）：2024-2025-2、2025-2026

代码通过 `len(doc.tables) >= 6` 自动检测。如果仍有问题，检查模板文件是否正确：
```bash
python -c "
from docx import Document
import os
tdir = '原始资料/模板/模板3：课程标准'
for f in sorted(os.listdir(tdir)):
    if f.endswith('.docx'):
        doc = Document(os.path.join(tdir, f))
        print(f'{f}: {len(doc.tables)}个表格')
"
```

### Q: 同一学期同一课程有多个专业/班级，文件被覆盖？
`get_output_path` 已自动检测同名课程并添加专业后缀。专业缩写映射：
- 农村电子商务 → 农商
- 全媒体广告策划与营销 → 全媒体
- 市场营销 → 营销

如需添加新专业的缩写，编辑 `generate.py` 中的 `major_abbrev` 字典。

### Q: 如何重新生成被覆盖的同名课程？
```bash
# 重新生成指定课程（会自动添加专业后缀）
python -c "
import store, generate
for oid in [22, 23, 29, 30]:  # 被覆盖的课程ID
    results = generate.generate_all(oid)
    for dt, r in results.items():
        if 'path' in r:
            print(f'ID={oid} {dt}: {r[\"path\"]}')
"
```

### Q: 如何批量重新生成所有课程的教学设计？
```bash
# 使用增强版 gen_design.py 重新生成所有教学设计
python -c "
import store, generate
offerings = store.rows(\"SELECT id, course_name, term FROM offerings WHERE offering_kind != '实训课程' ORDER BY id\")
for o in offerings:
    results = generate.generate_all(o['id'])
    r = results.get('design', {})
    if 'path' in r:
        print(f'ID={o[\"id\"]} {o[\"course_name\"]}: OK')
    elif 'error' in r:
        print(f'ID={o[\"id\"]} {o[\"course_name\"]}: {r[\"error\"]}')
"
# 生成后运行精修
python _batch_polish.py
```

### Q: 增强版 gen_design.py 与备份文档的差距？
增强版 gen_design.py 已集成原来 _fix_*.py 系列脚本的核心逻辑，文档结构已接近备份版：

| 对比项 | 备份版 | 增强版 | 差距 |
|--------|--------|--------|------|
| 文件大小 | 884KB | 737KB | 16% |
| 表格数 | 63 | 65 | +2 |
| 表5+6对 | 29对 | 30对 | +1 |
| 封面 | 有 | 有 | 已对齐 |
| 表0-4 | 已填充 | 已填充 | 已对齐 |
| AI内容 | 手动调优 | AI生成 | 内容质量有差距 |

**已知限制：** 模板表5/表6的合并单元格映射存在位置偏移，导致部分内容写入到了错误的单元格。后续需要分析模板合并布局并修正映射。
